# Mall-X 改良计划

> 生成日期：2026-09-06
> 依据：本地 `mall_x` MySQL 数据核查（13 表）、前端代码核查（`mall-frontend/src`）、`docs/implementation.md` 对照、本地冒烟结果 **23/23 PASS**（scripts/smoke.py）。
> 一句话结论：**后端链路功能基本齐全（里程碑 1-8 ✅，回调/幂等/限流已落地），但前端存在"接口已做、页面未接"的关键空档（购物车加购），视觉风格停留在"纸面票据"半成稿，且种子商品图片资源缺失导致页面呈现破碎。**

---

## 一、现状盘点（与 implementation.md 的偏差）

> 执行状态：**P0 / P1 / P2 + 第 2 波（M2-1~M2-7）+ 第 3 波 MinIO（M3-0~M3-3）全部完成（2026-09-06），P3 剩余环境项**（RocketMQ broker 上报地址/支付宝沙箱密钥/JUnit/压测）待办，见【执行记录】。

| # | 盘点项 | 代码/数据实况 | 与文档偏差 |
|---|---|---|---|
| 1 | 数据库数据 | `mall_x` 13 表均为真实数据：user 3 / merchant 2 / category 5 / product 3 / sku 5 / advert 2 / cart 0 / order 6 / order_item 6 / pay_info 6 / refund 1 / user_address 1 / withdrawal 4；种子来自 `docs/init.sql`（仅 10 处 INSERT），订单/支付/退款为冒烟脚本写出的真实流水 | **无前端 mock，全是真实库数据**；但商品仅 3 个、SKU 仅 5 个，演示量偏少 |
| 2 | 商品图片 | 商品 `main_img` 指向 `/img/product/phone.jpg` 等，但 **`mall-frontend/public` 下只有 favicon.svg**（无 img 目录） | 图片 404 → 首页/详情页大片空白 |
| 3 | 购物车 | 后端 `/api/portal/carts` 全家桶 ✅（GET/POST/merge/count/checked/delete）；`CartPage.vue` 完整（游客 localStorage + 登录合并 + 结算下单）✅；**但 `ProductDetail.vue:49-56` 的 `addCart()` 是占位 stub**，仅 `ElMessage.info('购物车功能在里程碑 5 接入')`，从未调用 `api/cart.ts` 中已存在的 `addToCart` | **文档 5.1 标记 ✅ 欠实**：加购入口未接线，导致 cart 表 0 条、购物车永远空 |
| 4 | 前台导航 | `Home.vue:66-76`：登录后显示"工作台"链接 → `/admin`；路由守卫（router/index.ts:52-59）要求 type 1/2 才能进 /admin，**普通用户（type=0）点击被静默重定向回首页** | "工作台点了没反应"的根因；前台登录后**没有退出登录入口** |
| 5 | 注册页 | `Register.vue` 已实现 `?role=merchant` 商家入驻分支（表单字段、文案、接口均正确） | 功能已实现，但**入口不直观**：首页"注册"默认普通用户，商家链接藏在登录页小字里，页面内无角色 tab 切换 |
| 6 | 视觉风格 | tokens.css 为"午夜蓝 + 琥珀金 + mono 数字"的纸面票据概念稿，卡片稀疏、无轮播大图、无障碍购物形态（无加购按钮到位的转化路径） | 需要对标淘宝式商城密度与转化导向重构 |
| 7 | 支付 | `PayPage.vue` 演示回调成功/失败；渠道 Mock（支付宝沙箱密钥留白） | 计划内（密钥待配），非 Bug |
| 8 | 环境 | RocketMQ broker 上报地址待确认（延迟消息/超时取消受此影响，下单已降级不阻断）；JUnit/压测未补 | 文档 9.1 已如实标注 ▶ |

---

## 二、待改良问题清单（按优先级）

### P0 —— 功能空档（用户可感知的"半成品"）

1. **商品详情页"加入购物车"未接线**
   - 位置：`mall-frontend/src/views/mall/ProductDetail.vue:49-56`
   - 现状：只弹提示 `已选规格：... —— 购物车功能在里程碑 5 接入`
   - 修正：游客 → 写入 localStorage 游客车（复用 CartPage 的 `mall_guest_cart` 结构）；登录用户 → 调 `api/cart.ts` 已有的 `addToCart(skuId, count)`，成功提示 + 顶部购物车角标更新
   - 卡点：样式类 `.spec_active` 判定（`specJson.includes(v)`）在多规格存在跨值匹配误选中，顺带改为精确匹配

2. **前台登录后无"退出登录"**
   - 位置：`Home.vue` 导航区（模板 66-76）
   - 修正：登录态下拉（头像/昵称 + 个人中心 + 我的订单 + 工作台[仅 type 1/2] + 退出登录），`stores/user.ts` 已有 `logout()` 直接用
   - 同时删除普通用户不该看到的"工作台"链接（或仅 type 1/2 渲染）

3. **注册页角色入口不直观**
   - 位置：`Register.vue` + `Home.vue` 导航"注册"链接
   - 修正：注册页顶部加"普通用户 / 商家入驻" tab（切换不影响已填表单），首页导航保留两个入口或下拉区分

4. **CartPage 规格列残句**
   - 位置：`CartPage.vue:170` `{{ row.specJson || '『规格' }}` —— 不完整文字，应显示已解析的规格文本或"单规格"

### P1 —— 数据与图片资源

5. **商品/广告图片资源缺失**
   - `public/img/` 不存在；`init.sql` 的 `main_img`、`img_url` 全是指向 `/img/...` 的假路径
   - 修正：补 3-6 张商品图 + 2 张广告位图（本地 SVG/PNG，或换用可靠占位图服务），上架图片走 `/img/商品/` 规整目录

6. **种子数据量太少**
   - 商品 3 / SKU 5 / 类目 5 / 广告 2 / 商家 2 —— 首页只有 3 张卡片
   - 修正：扩充到 12-16 个商品（每商家 2-3 类目覆盖）、每商品 2-6 SKU、广告 4 条、类目补后缀子类目，保证"类目走廊 + 广告条 + 推荐流"满铺

### P2 —— 视觉重构（对标淘宝）

7. **商城前台"购物形态"重构**（详见第三节设计规范）
   - 首页：通栏导航（Logo + 搜索 + 购物车角标）+ 轮播大图 + 类目走廊 + 商品网格（5 列）
   - 详情页：右列信息区（价格红色大数字 + 加购/立即购买双 CTA + 销量/库存/店铺入口）
   - 购物车：顶部全选 + 数量步进 + 结算栏（合计/去结算），去掉纯表格观感
   - 目标：一眼"这是电商",转化路径 首页→详情→加购→购物车→下单 每步有醒目 CTA

### P3 —— 环境与验证

8. RocketMQ broker 上报地址确认（192.168.193.131 broker.conf 挂载 + 重启后重测超时取消）
9. 支付宝沙箱密钥配置（application.yml 填入后切换真实渠道验证）
10. JUnit / 压测用例补齐（9.1 遗留）

---

## 三、前台视觉重构设计规范（对标淘宝）

> 采用 frontend-design 流程产出：先定方向 → token 系统 → 页面布局 → 签名元素。

### 3.1 设计方向

**"橙红货架"** —— 从"纸面票据"转向"中国电商大盘"的通用认知：**白底 + 橙红主色 + 高密度商品网格 + 价格红色加粗**。保留 Mall-X 的品牌唯一性在于**红章价签**（商品卡右下角 MX 圆章，微渐变橙红），让"多商家价签"概念融入现代电商形态，而不是一套模板化的淘宝复制品。

### 3.2 Token 系统（替换 tokens.css 中"午夜蓝/琥珀金"）

| Token | 值 | 用途 |
|---|---|---|
| 品牌红 `--mx-primary` | `#FF5000` | 主 CTA、价格数字、Logo、链接 hover |
| 红深 `--mx-red-deep` | `#E23E00` | hover/按压态、角标 |
| 墨色 `--mx-ink` | `#1D2129` | 标题、正文 |
| 次灰 `--mx-ink-2` | `#6B7280` | 副文本、标签 |
| 底白 `--mx-bg` | `#FFFFFF` | 页面底 |
| 灰底 `--mx-bg-2` | `#F5F6F7` | 卡片底、分区底 |
| 提示黄 `--mx-warn` | `#FFB800` | 促销标签、限时 |
| 成功绿 `--mx-ok` | `#00B578` | 库存充裕、状态点 |

### 3.3 字体

- 显示/标题：`PingFang SC` / `Microsoft YaHei UI` 700，替代 mono 票据数字（价格保留 `font-variant-numeric: tabular-nums` 对齐）
- mono 仅保留于"订单号/单号"等数据场景（`--mx-mono`: `ui-monospace`），其余全部回归中文黑体栈

### 3.4 页面布局（ASCII 草图）

```
┌──────────────────────────────────────────────────────────┐
│ 顶栏: MX🔴 logo │ 搜索框(居中, 大) │ 购物车🛒(角标) 我的订单 │ 登录│注册 ┤
├──────────────────────────────────────────────────────────┤
│ 类目走廊: 手机数码 家用电器 服饰鞋帽 ...(通栏浅灰底)             │
├──────┬───────────────────────────────────────────────────┤
│      │  轮播大图(广告位1, 全宽, 3 帧, 圆角)                  │
│ 侧   ├───────────────────────────────────────────────────┤
│ 栏   │  广告位2 · 广告位3 (横幅双卡)                          │
│      ├───────────────────────────────────────────────────┤
│      │  商品推荐流 (5 列网格, 卡片: 图 / 标题2行 / 红价+销量 / MX章) │
└──────┴───────────────────────────────────────────────────┘

详情页:
┌────────────┬──────────────────────────────────────────┐
│ 左侧大图     │ 标题(20px 700) · 副标题                   │
│ (圆角, 灰底)  │ ────────────────────────────             │
│            │ ¥199.00 (48px 红) 促销标签 │ 库存 500 ✔     │
│            │ 颜色/规格 pills (选中橙红描边)              │
│            │ [加入购物车(橙红大按钮)] [立即购买(描边)]     │
│            │ 店铺卡: 🏪 极客数码旗舰店 >                    │
└────────────┴──────────────────────────────────────────┘
```

### 3.5 签名元素

- **MX 红章价签**：商品卡右下角一个 `45° 椭圆红章`（渐变 `#FF5000→#E23E00`），内嵌 mono 价签数字，延续"多商家独立价签"叙事 —— 这是整站区别于任何模板电商的唯一记忆点
- 轮播播放进度点 + 卡片 hover 上浮 2px + 阴影加深（保留现有交互习惯）

---

## 四、改良执行顺序与验收

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| 第 1 波（1-2 天） | P0 全部：加购接线、退出登录、注册 tab、CartPage 残句 | 游客加购→本地车→登录合并→下单全链；管理员/商家/用户三身份退出路径清晰 |
| 第 2 波（1-2 天） | P1 全部：图片资源 + 种子数据扩充 | 首页图片全显，商品≥12，类目/广告满铺；重跑 smoke.py 23/23 仍 PASS |
| 第 3 波（2-3 天） | P2 视觉重构：按 3.2-3.5 规范改造 Home / Detail / Cart / Login / Register / Profile | 桌面 1160 + 移动 720 断点均完整；无图裂、无溢出；截图对照淘宝观感 |
| 第 4 波（随缘） | P3 环境：RocketMQ 上报地址、支付宝密钥、JUnit/压测 | 超时取消真实生效；支付走沙箱签名验签；压测 QPS 数据可用 |

> 每波完成后更新本文件【执行记录】小节，并同步 `docs/implementation.md` 对应里程碑状态。

---

## 五、执行记录

| 日期 | 内容 |
|---|---|
| 2026-09-06 | **P0 四项全部完成**：① 详情页加购接线（游客 localStorage `mall_guest_cart` / 登录调 `addToCart`），规格选择器改为按键值精确匹配（`selectedSpec`），不再 `includes` 子串误选中；② 前台导航登录态改 el-dropdown（个人中心/我的订单/工作台[仅 type 1/2]/退出登录），购物车角标数量；③ 注册页顶部"普通用户/商家入驻"tab（同步 URL query）；④ CartPage 规格列 `'『规格` 残句改 `formatSpec()`（JSON→"颜色：白色；尺码：L"）。vue-tsc 全绿 |
| 2026-09-06 | **P1 完成**：① `scripts/gen_images.py`（Pillow）生成 12 张商品图（浅灰渐变底+产品剪影+阴影）、4 张橙红广告横幅、3 张类目圆底图标，落位 `mall-frontend/public/img/{product,ad,cat}`，修复此前 `/img/*` 全 404；② 种子数据扩充：商品 3→13（商家 1 数码 4-8 / 商家 2 服饰 9-13）、SKU 5→23（覆盖颜色/尺码/存储/轴体多维规格）、广告 2→4，同步更新 `docs/init.sql`；`/api/portal/products` 核验 total=13 |
| 2026-09-06 | **P2 完成**（按 3.2-3.5 规范）：① `tokens.css` 主色换 `#FF5000` 橙红（`--mx-*` 新令牌 + Element Plus 主色联动，后台沿用旧名保留旧观感）；② Home 重构：橙红方标 Logo + 双边框搜索栏、类目走廊 pill 选中橙红、**广告位轮播（自动 4.2s + 指示点 + 悬停暂停 + 点击跳转对应类目）**、小广告卡横条、5 列商品网格（2 行标题截断、店铺 MX 角标、橙红大字价+已售）；③ ProductDetail：橙红渐变价格盒（42px 大字+库存）、规格 pill 选中橙红描边、"加入购物车/立即购买"双圆角 CTA、店铺卡、商铺标签；④ CartPage：顶部购物车列表（勾选/缩略图/数量步进/小计/删除），全选联动，sticky 底部结算栏（合计大字+去结算按钮），下单成功弹窗每笔订单"去支付"直达 PayPage；⑤ 后端无改动。vue-tsc 全绿 |
| 2026-09-06 | **M2-1 完成：结算"库存不足"修复**。根因：order-service 早于种子扩充启动，StockWarmer 启动预热只同步了当时 5 个 SKU；新增 18 个 SKU 在 Redis 中无 `stock:{id}` key，Lua 预扣返回 `-1(un-warmed)` → 体验为"库存不足"。修复：StockWarmer 增加 `@Scheduled` 每 2 分钟补预热缺失 key（不覆盖扣减中间值），OrderApplication 加 `@EnableScheduling`；重启服务后预热 23 SKU，实测新 SKU（sku=22）下单成功。附注：Windows 下 curl 传中文被 GBK 转码会致 JSON 解析 500，与业务无关，改用 ASCII/请求库 |
| 2026-09-06 | **M2-2 完成：商家审核"用户存在"修复**。根因：雪花 ID（19 位，如 `2096571827303944193`）超过 JS `MAX_SAFE_INTEGER`（9e15），前端默认 `JSON.parse` 丢精度 → 审核请求 URL 变 `.../2096571827303944200/audit`，后端查无 → 报"商家不存在/用户已存在"。修复：前端安装 `json-bigint`（`storeAsString`），`http.ts` 全局 `transformResponse` 安全解析，雪花 ID 以字符串传输；`user store` id 改 string、`report.getDashboard` 接受 string、Home 类目比较改 String() 归一。验证：newseller 注册→管理员审核通过→商家登录 /admin 全链 PASS |
| 2026-09-06 | **M2-3 完成：商品查询分页**。首页商品网格下加 el-pagination（prev/pager/next/total，12/页），翻页回滚到网格；切类目/搜索重置回第一页；加载商品时 `currentPage` 参与请求 |
| 2026-09-06 | **M2-4 完成：我的订单页信息丰富**。顶部新增状态统计栏（全部/待支付/已支付/已发货/已收货/已取消 计数卡）；订单卡改两栏：左商品行（emoji 图标兜底/标题/可读规格/数量/金额），右信息栏（金额/收货人脱敏/地址/物流单号/发货时间/签收时间）；底部"共 N 件商品 + 实付红字 + 操作按钮"，统一橙红视觉 |
| 2026-09-06 | **M2-5 完成：个人中心信息丰富**。新增顶部橙红渐变用户卡（头像首字/昵称/@用户名/地址数/默认地址/手机绑定状态统计）；收货地址由表格改为卡片栅格（默认标签/电话/地址全文/编辑删除），支持"＋添加收货地址"空态引导 |
| 2026-09-06 | **M2-6 验证全绿**：新增 `scripts/playwright_walk2.py` 走查 **9/9 PASS**（分页 2 页/第 2 页 1 件、订单统计栏与卡片、个人中心用户卡、结算→下单→支付全链、审核列表）。过程发现并修复 **PayPage 模拟支付金额写死 1** 导致回调金额不一致被拒（改为从订单接口取 `payAmount`）；`vue-tsc` 0 错；`smoke.py` 23/23 无回归。截图 `shots/09-12` |
| 2026-09-06 | **M2-7 完成：注册"系统繁忙"修复**（一石三鸟）。① 根因：`Register.vue submit()` 误写 `if (isMerchant)`（ref 对象恒真），普通用户注册也调商家接口 → 商家接口校验 phone 失败 → 全局异常吞成"系统繁忙"；改为 `isMerchant.value`。② 全局异常处理器 `GlobalExceptionHandler` 增加 `MethodArgumentNotValidException/BindException` 分支，校验失败返回真实字段信息（如"联系电话不能为空"）而非 500 吞错误。③ 验证四场景：普通注册/商家注册/缺字段提示/切 tab 后注册全部正常，`vue-tsc` 0 错；auth-service 重打包重启（端口 8010，PID 10916） |
| 2026-09-06 | **M4-2 完成：上传"服务器繁忙"修复 + el-upload 样式升级**。根因：Spring Boot 默认 multipart 上限 1MB，用户传真实照片超限 → `MaxUploadSizeExceededException` 被全局处理器吞成"系统繁忙"。修复：① product/merchant yml 加 `spring.servlet.multipart.max-file-size/max-request-size: 20MB`；② `GlobalExceptionHandler` 增 `MaxUploadSizeExceededException` 分支返回"文件过大，请选择 20MB 以内的图片"；③ `ImageUpload.vue` 重构为 **Element Plus el-upload** 形态（上传区+预览+URL 回填+类型/大小前置校验+提示文案），安装 `@element-plus/icons-vue`。验证：11MB 大图接口 200（原先 500）、正常图 200、浏览器 el-upload 点击上传成功并回填 URL；vue-tsc/vite build 全绿；merchant/product/gateway 重启 |
| 2026-09-06 | **M4 完成：全站图片上传化（店铺 logo/收款码/广告图）**。① 新增通用前端组件 `components/ImageUpload.vue`（预览+上传+URL 回填，`upload` 函数注入），商品主图也复用改造。② 后端上传端点：merchant-service `POST /api/merchant/upload/image`（type=2 店铺 logo/收款码）、product-service `POST /api/admin/upload/image`（type=1 广告图），网关路由补 `/api/admin/upload/**`；merchant yml 加 `mall.minio` 配置。③ **关键修复**：`MinioConfig` 改为纯 properties + 新增 `MinioConfiguration`（`@ConditionalOnProperty(mall.minio.endpoint)` 条件装配）+ `MinioUploader` `@ConditionalOnBean` —— 解决网关（webflux 无 minio 配置）扫描 common 时 `endpoint must not be null` 启动失败的坑；**打包踩坑**：`-pl X package` 不带 `-am` 时 D:\repo 依赖未刷新（`rm -rf target` + 重装 common 解决）。④ 验证：浏览器商家上传 logo/收款码→保存、管理员上传广告图→前台轮播显示（naturalWidth>0），数据库全部只存 MinIO URL；`vue-tsc`/`vite build` 0 错；merchant/product/gateway 重启正常 |
| 2026-09-06 | **M3-0~M3-3 完成：MinIO 对象存储接入**。① **M3-0 环境**：MinIO 已于 192.168.193.131:9000/9001 运行（账号 `root/12345678`，用户提供），mall-x 桶创建（桶存在判断→不存在建桶，已设公开读策略 s3:GetObject 供浏览器直显）。② **M3-1 封装**：`mall-common` 新增 `MinioConfig`（`mall.minio.*` 配置：endpoint/access-key/secret-key/bucket/public-url）+ `MinioUploader`（`ensureBucket` 桶存在判断+自动创建，`upload` 上传返回公开 URL，`{bucket}/{yyyy/MM}/{uuid}.{ext}`），pom 引 minio 8.5.12；product-service `application.yml` 追加配置。③ **M3-2 接入**：`ProductUploadController`（`POST /api/product/upload/image` 商家 type=2，图片类型校验），前端 `MerchantProducts.vue` 表单"主图"上传组件（预览+上传按钮+URL 回填），API `uploadProductImage`；**数据库只存 URL 字符串**（验证 product.id=8 main_img 为 `http://192.168.193.131:9000/mall-x/...`）。④ **M3-3 验证**：浏览器商家上传→保存→前台详情页+首页列表图可加载（naturalWidth>0,HTTP 200 无 403），`vue-tsc` 0 错；product-service 重打包重启（8013，含 MinIO 初始化日志） |
| 2026-09-06 | **走查验证全部通过**：首次 Playwright 走查（`scripts/playwright_walk.py`）暴露 1 个真实 Bug —— 轮播未启动（`loadAdverts` 完成后未调 `startBanner`），已修复；认证守卫跳转经 `wait_for_url` 核验实际正常（此前 FAIL 为脚本时序误报）。**最终 15/15 PASS**：首页商品卡 12 + 轮播 4 + 自动切换 / 游客加购写 localStorage / 购物车页显示 / 登录合并（cart 表持久化） / 角标数量 2 / 下拉含退出登录 / 退出清空 token / 注册 tab 切换 / 管理员工作台入口 + 跳转 / 无 console 错误。**后端 smoke.py 23/23 PASS 无回归**。截图存 `scripts/shots/01-07` |
